{
    "name": "SSH Server",
    "description": "Version par défaut",
    "ways": [
        {
            "name": "apt",
            "testCmd": "apt -v",
            "commands": [
                "apt install -y openssh-server"
            ]
        },
        {
            "name": "pacman",
            "testCmd": "pacman -V",
            "commands": [
                "pacman -S openssh --noconfirm"
            ]
        }
    ]
}