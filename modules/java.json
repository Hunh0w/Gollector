{
    "name": "Java (Default Version)",
    "description": "Version par défaut de Java",
    "ways": [
        {
            "name": "apt",
            "testCmd": "apt -v",
            "commands": [
                "apt install -y default-jre"
            ]
        },
        {
            "name": "pacman",
            "testCmd": "pacman -V",
            "commands": [
                "pacman -S jre-openjdk --noconfirm"
            ]
        }
    ]
}