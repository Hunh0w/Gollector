{
    "name": "Docker",
    "description": "Version par défaut",
    "ways": [
        {
            "name": "apt",
            "testCmd": "apt -v",
            "commands": [
                "apt install -y docker.io",
                "docker -v"
            ]
        },
        {
            "name": "pacman",
            "testCmd": "pacman -V",
            "commands": [
                "pacman -S docker --noconfirm",
                "docker -v"
            ]
        }
    ]
}