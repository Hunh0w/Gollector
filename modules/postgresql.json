{
    "name": "PostgreSQL",
    "description": "Version par défaut de PostgreSQL",
    "ways": [
        {
            "name": "apt",
            "testCmd": "apt -v",
            "commands": [
                "apt install -y postgresql",
                "echo 'Installation finished !'"
            ]
        },
        {
            "name": "pacman",
            "testCmd": "pacman -V",
            "commands": [
                "pacman -S postgresql --noconfirm"
            ]
        }
    ]
}