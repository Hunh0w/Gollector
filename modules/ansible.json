{
    "name": "Ansible",
    "ways": [
        {
            "name": "apt",
            "testCmd": "apt -v",
            "commands": [
                "apt install -y ansible",
                "echo 'coucou les gens'"
            ]
        },
        {
            "name": "pacman",
            "testCmd": "pacman -V",
            "commands": [
                "pacman -S ansible --noconfirm",
                "prout"
            ]
        }
    ]
}