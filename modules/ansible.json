{
    "name": "Ansible",
    "description": "Version par défaut de Ansible",
    "ways": [
        {
            "name": "pacman",
            "testCmd": "pacman -V",
            "commands": [
                "pacman -S ansible --noconfirm"
            ]
        },
        {
            "name": "apt",
            "testCmd": "apt -v",
            "commands": [
                "apt install -y ansible"
            ]
        }
        
    ]
}